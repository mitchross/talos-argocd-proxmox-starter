# Secrets

**This directory is no longer used.**

Secrets are now managed by **1Password Connect + External Secrets Operator (ESO)**.

See [secrets-example.md](../secrets-example.md) for setup instructions.

The YAML files in this directory are kept as reference for the secret structure,
but are not applied manually. ESO creates these secrets automatically from 1Password.
