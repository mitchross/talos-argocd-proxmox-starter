apiVersion: v1
kind: Secret
metadata:
  name: kopia-credentials
  namespace: volsync-system
type: Opaque
stringData:
  # CHANGE: Set a strong password for Kopia repository encryption
  # This password encrypts all backup data. Keep it safe!
  KOPIA_PASSWORD: "CHANGE-ME-kopia-encryption-password"
