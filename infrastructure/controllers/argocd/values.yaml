configs:
  cm:
    create: true
    application.resourceTrackingMethod: "annotation+label"
    kustomize.buildOptions: "--enable-helm"
    # Enable Application health check for sync waves to work properly
    # Without this, ArgoCD considers Application resources immediately healthy
    # and all sync waves deploy simultaneously (defeating the ordering)
    resource.customizations.health.argoproj.io_Application: |
      hs = {}
      hs.status = "Progressing"
      hs.message = ""
      if obj.status ~= nil then
        if obj.status.health ~= nil then
          hs.status = obj.status.health.status
          if obj.status.health.message ~= nil then
            hs.message = obj.status.health.message
          end
        end
      end
      return hs
    # Custom health checks for VolSync resources
    resource.customizations.health.volsync.backube_ReplicationSource: |
      hs = {}
      if obj.status ~= nil then
        if obj.status.lastSyncTime ~= nil then
          hs.status = "Healthy"
          hs.message = "Last sync: " .. obj.status.lastSyncTime
          return hs
        end
      end
      hs.status = "Progressing"
      hs.message = "Waiting for first sync"
      return hs
    resource.customizations.health.volsync.backube_ReplicationDestination: |
      hs = {}
      if obj.status ~= nil and obj.status.latestImage ~= nil then
        hs.status = "Healthy"
        hs.message = "Ready for restore"
        return hs
      end
      hs.status = "Progressing"
      hs.message = "Initializing"
      return hs
  params:
    server.insecure: true

crds:
  install: true
  # -- Keep CRDs on chart uninstall
  keep: false

controller:
  resources:
    requests:
      cpu: 100m
      memory: 700Mi
    limits:
      cpu: 4000m
      memory: 2Gi

dex:
  resources:
    requests:
      cpu: 20m
      memory: 64Mi
    limits:
      cpu: 1000m
      memory: 256Mi

redis:
  resources:
    requests:
      cpu: 100m
      memory: 64Mi
    limits:
      cpu: 2000m
      memory: 128Mi

server:
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 4000m
      memory: 256Mi

repoServer:
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 4000m
      memory: 1024Mi

applicationSet:
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 1000m
      memory: 512Mi

notifications:
  enabled: false
  resources:
    requests:
      cpu: 100m
      memory: 64Mi
    limits:
      cpu: 1000m
      memory: 128Mi
