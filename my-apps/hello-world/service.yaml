apiVersion: v1
kind: Service
metadata:
  name: hello-world
spec:
  selector:
    app: hello-world
  ports:
  - name: http # named port — HTTPRoute backends fail silently without one
    port: 8080
    targetPort: 8080
